-- Rest time between sets: planned target per exercise-in-workout, and the
-- actual rest the athlete took after each logged set (may differ from plan).
alter table public.workout_exercises add column if not exists rest_seconds integer;
alter table public.set_logs add column if not exists rest_taken_seconds integer;